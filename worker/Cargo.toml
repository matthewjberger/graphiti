[package]
name = "worker"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
graphiti = { path = ".." }
protocol = { path = "../protocol" }
nightshade = { version = "0.57.0" }
nightshade-api = { version = "0.57.0", default-features = false, features = [
    "offscreen",
] }
serde_json = "1"
wasm-bindgen = "=0.2.115"
